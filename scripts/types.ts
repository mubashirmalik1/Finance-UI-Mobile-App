export interface ExpenseList {
    id: String;
    name: String;
    price: String;
    percentage:String;
}

export interface IncomeList {
  id: number;
  name: string;
  amount: string;
}

export interface SpendingList {
  id: number;
  name: string;
  amount: string;
  date: string;
}